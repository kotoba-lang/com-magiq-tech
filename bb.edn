{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([magiq_tech.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'magiq_tech.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
